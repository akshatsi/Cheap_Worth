{
  "id": "quicksort",
  "difficulty": "hard",
  "spec": "Write a function `quicksort(arr)` that returns a new list containing the elements of `arr` sorted in ascending order, implemented using the quicksort algorithm (partition around a pivot, recurse on each side). Must handle an empty list, a list with duplicate values, and a list that is already sorted, without raising an error or recursing indefinitely.",
  "tests": "def test_quicksort_basic():\n    assert quicksort([3, 1, 2]) == [1, 2, 3]\n\n\ndef test_quicksort_empty():\n    assert quicksort([]) == []\n\n\ndef test_quicksort_duplicates():\n    assert quicksort([5, 3, 5, 1, 3]) == [1, 3, 3, 5, 5]\n\n\ndef test_quicksort_already_sorted():\n    assert quicksort([1, 2, 3, 4]) == [1, 2, 3, 4]\n\n\ndef test_quicksort_does_not_mutate_input():\n    original = [3, 1, 2]\n    quicksort(original)\n    assert original == [3, 1, 2]\n"
}
